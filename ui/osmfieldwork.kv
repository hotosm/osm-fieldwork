MDBoxLayout:
    orientation: 'vertical'
    padding: dp(10)

    MDBoxLayout:
        adaptive_height: True

        MDTextField:
            id: fld
            hint_text: 'Search'

        MDIconButton:
            icon: 'magnify'
            on_release: app.download_basemap()
