name: Build CI Img

on:
  # Push includes PR merge
  push:
    branches:
      - main
    paths:
      # Workflow is triggered only if deps change
      - "pyproject.toml"
      - "Dockerfile"
  # Allow manual trigger
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  GIT_BRANCH: ${{ github.ref_name }}

jobs:
  build-and-push-images:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name }}
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: extract_api_version
        run: |
          echo "PKG_VERSION=$(python -c 'from osm_fieldwork.__version__ import __version__; print(__version__)')" >> $GITHUB_ENV

      - name: Build image
        uses: docker/build-push-action@v4
        with:
          context: .
          target: ci
          push: true
          tags: |
            "ghcr.io/hotosm/osm-fieldwork:${{ env.PKG_VERSION }}-ci"
            "ghcr.io/hotosm/osm-fieldwork:ci"
          build-args: |
            PKG_VERSION=${{ env.PKG_VERSION }}
